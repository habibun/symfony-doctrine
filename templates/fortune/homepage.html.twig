{% extends 'base.html.twig' %}

{% block body %}

    <h2>Category</h2>

    <table>
        <thead>
            <th>Name</th>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.name }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}

{% block title %}

{% endblock %}

